import XCTest
@testable import DemoNielsenPackage

final class DemoNielsenPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DemoNielsenPackage().text, "Hello, World!")
    }
}
